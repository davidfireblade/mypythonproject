pipeline {
    agent any
    environment {
        APPLICATION_NAME = 'mypythonproject'
        GIT_REPO="https://github.com/davidfireblade/mypythonproject.git"
    }
    stages {
        stage('Build') {
            steps {
                script {
                    openshift.withCluster() {
                        openshift.withProject() {
                            def builds = openshift.selector("bc", 'mypythonproject').related('builds')
                            builds.untilEach(1) {
                                return (it.object().status.phase == "Complete")
                            }
                        }
                    }
                } // script
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh('''python --version;''')
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                script {
                    openshift.withCluster() {
                        openshift.withProject() {
                            def rm = openshift.selector("dc", 'mypythonproject').rollout()
                            timeout(5) { 
                                openshift.selector("dc", 'mypythonproject').related('pods').untilEach(1) {
                                    return (it.object().status.phase == "Running")
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}